extends ../layout/app.pug

block content
  div(class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 mt-12 mb-12")
    
    // Mensaje de éxito
    div(class="bg-green-50 border border-green-200 rounded-xl p-8 mb-8 text-center")
      svg(xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-16 h-16 mx-auto mb-4 text-green-600")
        path(stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z")
      h2(class="text-3xl font-bold text-gray-900 mb-2") ¡Pedido Confirmado!
      p(class="text-gray-700") Tu pedido ha sido creado exitosamente.

    // Detalles del pedido
    div(class="bg-white rounded-xl shadow-md p-8")
      
      // Número de pedido y fecha
      div(class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6 pb-6 border-b border-gray-200")
        div
          h6(class="text-xs font-medium text-gray-500 uppercase mb-2") Número de Pedido
          h3(class="text-2xl font-bold text-orange-600")= `#${pedido.id}`
        div(class="md:text-right")
          h6(class="text-xs font-medium text-gray-500 uppercase mb-2") Fecha
          p(class="text-lg font-semibold text-gray-900")= new Date(pedido.fecha_creacion).toLocaleDateString('es-CO')

      // Dirección y método de pago
      div(class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6 pb-6 border-b border-gray-200")
        div
          h6(class="text-xs font-medium text-gray-500 uppercase mb-2") Dirección de Envío
          p(class="text-gray-900")= pedido.direccion_envio
        div
          h6(class="text-xs font-medium text-gray-500 uppercase mb-2") Método de Pago
          p(class="text-gray-900")= pedido.metodo_pago === 'contra_entrega' ? 'Contra Entrega' : pedido.metodo_pago === 'tarjeta' ? 'Tarjeta de Crédito' : 'Transferencia Bancaria'

      // Estado del pedido
      div(class="mb-6 pb-6 border-b border-gray-200")
        h6(class="text-xs font-medium text-gray-500 uppercase mb-3") Estado del Pedido
        div(class="flex items-center gap-3")
          span(class="inline-flex items-center px-4 py-2 rounded-full text-sm font-semibold bg-blue-100 text-blue-800")
            | Pendiente
          div(class="flex-1 bg-gray-200 rounded-full h-2")
            div(class="bg-blue-600 h-2 rounded-full" style="width: 25%;")

      // Total
      div(class="mb-6")
        div(class="flex justify-between items-center")
          span(class="text-xl font-bold text-gray-900") Total del Pedido:
          span(class="text-3xl font-bold text-orange-600") $#{total}

      // Mensaje informativo
      div(class="bg-blue-50 border border-blue-200 rounded-xl p-4")
        p(class="text-blue-800 text-sm") Te enviaremos un email de confirmación con los detalles de tu pedido. Puedes seguir el estado en tu panel de usuario.

    // Botones de acción
    div(class="flex flex-col sm:flex-row gap-4 justify-center mt-8")
      a(href="/" class="inline-flex items-center justify-center px-6 py-3 bg-orange-600 text-white font-semibold rounded-full hover:bg-orange-700 transition cursor-pointer")
        | Volver al Inicio
      a(href="/productos" class="inline-flex items-center justify-center px-6 py-3 border border-orange-200 text-orange-700 font-semibold rounded-full hover:bg-orange-50 transition")
        | Seguir Comprando

extends ../layout/index

block contenido
    // Contenedor Principal con dos columnas - Full screen sin paddings
    div(class="min-h-screen bg-white flex absolute inset-0")
        
        // Lado Izquierdo - Información
        div(class="hidden lg:flex lg:w-1/2 bg-gradient-to-br from-orange-50 to-orange-100 flex-col justify-center px-12")
            div(class="max-w-md")
                h2(class="text-6xl font-bold text-gray-900 mb-4")
                    | Únete a 
                    span(class="text-orange-600") OpenTrade
                p(class="text-lg text-gray-700 mb-6")
                    | Crea tu cuenta y comienza a comprar o vender productos. Es rápido, fácil y seguro.
                
                div(class="space-y-4")
                    div(class="flex items-start")
                        svg(class="w-6 h-6 text-orange-600 mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20")
                            path(fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd")
                        p(class="text-gray-700") Registro gratuito y rápido
                    
                    div(class="flex items-start")
                        svg(class="w-6 h-6 text-orange-600 mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20")
                            path(fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd")
                        p(class="text-gray-700") Publica tus productos sin costo
                    
                    div(class="flex items-start")
                        svg(class="w-6 h-6 text-orange-600 mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20")
                            path(fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd")
                        p(class="text-gray-700") Acceso a todas las categorías

        // Lado Derecho - Formulario
        div(class="w-full lg:w-1/2 flex flex-col justify-center px-6 sm:px-12 lg:px-16")
            div(class="max-w-md w-full mx-auto")
                
                h1(class="text-3xl font-bold text-orange-600 mb-2") Crear Cuenta
                p(class="text-gray-600 mb-8") Completa el formulario para registrarte

                if errores
                    div(class="mb-6 p-4 bg-red-50 border border-red-200 text-red-700 rounded-lg")
                        each error in errores
                            p(class="text-sm font-medium")= error.msg

                form(method="POST" action="registro" noValidate class="space-y-5")
                    input(type="hidden" name="_csrf" value=csrfToken)

                    // Nombre
                    div
                        label(class="block text-sm font-medium text-gray-700 mb-2" for="nombre") Nombre completo
                        input#nombre(
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-transparent outline-none transition" 
                            type="text" 
                            name="nombre" 
                            placeholder="Tu nombre completo" 
                            required
                        )

                    // Email
                    div
                        label(class="block text-sm font-medium text-gray-700 mb-2" for="email") Correo electrónico
                        input#email(
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-transparent outline-none transition" 
                            type="email" 
                            name="email" 
                            placeholder="tu@correo.com" 
                            required
                        )

                    // Contraseña
                    div
                        label(class="block text-sm font-medium text-gray-700 mb-2" for="password") Contraseña
                        input#password(
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-transparent outline-none transition" 
                            type="password" 
                            name="password" 
                            placeholder="Mínimo 6 caracteres" 
                            required
                        )

                    // Confirmar Contraseña
                    div
                        label(class="block text-sm font-medium text-gray-700 mb-2" for="passwordConfirm") Confirmar contraseña
                        input#passwordConfirm(
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-transparent outline-none transition" 
                            type="password" 
                            name="repeat_password" 
                            placeholder="Repite tu contraseña" 
                            required
                        )

                    // Botón Registrarse
                    button(
                        type="submit" 
                        class="w-full bg-orange-600 hover:bg-orange-700 text-white font-semibold py-3 rounded-lg transition duration-200"
                    ) Crear cuenta

                // Divider
                div(class="relative my-6")
                    div(class="absolute inset-0 flex items-center")
                        div(class="w-full border-t border-gray-300")
                    div(class="relative flex justify-center text-sm")
                        span(class="px-2 bg-white text-gray-500") O

                // Enlaces auxiliares
                div(class="text-center text-sm")
                    p(class="text-gray-600")
                        | ¿Ya tienes cuenta? 
                        a(class="text-orange-600 hover:text-orange-700 font-medium" href="/auth/login") Inicia sesión aquí